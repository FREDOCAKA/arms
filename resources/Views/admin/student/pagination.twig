
<nav aria-label="...">
  <ul class="pagination pagination-sm">

  {%  if items.currentPage == 1 %}
     <li class="page-item disabled">
         <a class="page-link" href="" tabindex="-1">Previous</a>
    </li>

    <li class="page-item active">
      <a class="page-link" href="{{ path_for('student.index')}}?page={{ items.currentPage }}">{{ items.currentPage }} <span class="sr-only">(current)</span></a>
    </li> 

    {% if items.hasMorePages == 1 %}
      {# <li class="page-item">
        <a class="page-link" href="{{path_for('student.index')}}?page={{items.currentPage + 1}}">
          {{ items.currentPage + 1}}
        </a>
      </li> #}
        
      <li class="page-item">
        <a class="page-link" href="{{path_for('student.index')}}?page={{items.currentPage + 1}}">
          Next
        </a>
      </li>

    {% endif %}


  {% elseif  items.hasMorePages == 0 %}

   <li class="page-item ">
         <a class="page-link" href="{{path_for('student.index')}}?page={{items.currentPage - 1}}" tabindex="-1">
         Previous
         </a>
    </li>


    {# <li class="page-item">
      <a class="page-link" href="{{path_for('student.index')}}?page={{items.currentPage  - 1}}">
         {{ items.currentPage - 1}}
      </a>
    </li> #}



  <li class="page-item active">
      <a class="page-link" href="{{ path_for('student.index')}}?page={{ items.currentPage }}">
       {{ items.currentPage }}
       <span class="sr-only">(current)</span></a>
    </li>

     <li class="page-item disabled">
      <a class="page-link" href="">
        Next
      </a>
    </li>

  {% else %}

    <li class="page-item ">
         <a class="page-link" href="{{path_for('student.index')}}?page={{items.currentPage - 1}}" tabindex="-1">
          Previous
         </a>
    </li>


      <li class="page-item active">
      <a class="page-link" href="{{ path_for('student.index')}}?page={{ items.currentPage }}">{{ items.currentPage }}
       <span class="sr-only">(current)</span></a>
    </li>
     
{# 
    <li class="page-item">
      <a class="page-link" href="{{path_for('student.index')}}?page={{items.currentPage + 1}}">
      {{ items.currentPage + 1}}
      </a>
    </li> #}


     <li class="page-item">
      <a class="page-link" href="{{path_for('student.index')}}?page={{items.currentPage + 1}}">
        Next
      </a>
    </li>

  {% endif  %}
  </ul>
</nav>

